# 1. Transformer架构
Transformer 是现代深度学习中最核心的架构之一，几乎当前所有大语言模型（GPT 系列、LLaMA、Qwen 等）都建立在它的基础上。它最初由 Google 在 2017 年的论文《Attention is All You Need》中提出，用来解决**机器翻译**问题，但很快就被发现是一个通用的序列建模神器。

要理解 Transformer，最好的切入点是先看看它**要解决什么问题**，以及它**最关键的创新**——自注意力（Self‑Attention）。

---

## 1. 在 Transformer 之前，我们用什么处理文本？

文本是**序列**——单词一个接一个。2017 年以前的主流模型是 **RNN（循环神经网络）** 和 **LSTM**。它们像人阅读一样，从左到右逐个处理单词：
  
- 处理第 t 个单词时，必须等前 t‑1 个单词都处理完，得到一个“隐藏状态”，再把当前单词和隐藏状态结合起来。
    
- 优点：很符合直觉，天然适合序列。
    
- 缺点：**串行计算**，没办法并行，训练很慢。而且处理长句子时，前后单词间的信息传递路径太长，容易“忘记”开头的内容（长距离依赖问题）。
    

## 2. Transformer 的革命性想法：让所有单词同时“开会”

Transformer 提出一个完全不同的思路：

> **不要一个一个处理单词，让整个句子里的所有单词一次性、两两交互，看看谁和谁关系更密切。**

这就像一个会议：每个单词都是==参会者==，它们同时发言、同时倾听所有其他单词说了什么，然后根据听到的信息调整自己要表达的内容。这套机制就叫做 **自注意力（Self‑Attention）**。

因为所有单词是同时处理的，不再有串行依赖，所以 ==**可以完全并行计算**==，训练速度成百倍地提高，而且“开会”机制让任意两个单词的距离都是 1，完美解决了长距离依赖。
Transformer架构图：
---
![[Pasted image 20260604155148.png]]
![[Pasted image 20260604162419.png]]
## 3. 自注意力具体是怎么算的？

我们可以把每个单词先转换成一个向量（叫做 **hidden state** 或 **embedding**），维度通常是 768 或更大。  
==自注意力计算==需要从每个单词的向量里“抽取”出三种角色：

- **Query（Q，查询）**：我要找什么？
    
- **Key（K，键）**：我能提供什么？
    
- **Value（V，值）**：如果我被选中了，我会输出什么内容？
    

它们都是由同一个单词向量乘上三个不同的权重矩阵（可学习参数）得到的。
![[Pasted image 20260604160726.png]]
例如句子 “我 今天 吃 了 一碗 饭 呢”，它有 7 个 token。我们会为每个 token 都算出一组 Q、K、V，形状都是 `(5, head_dim)`。

然后进行核心计算：
	$Q = XW_Q$, $K = XW_K$, $V = XW_V$
	

1. **算注意力分数**：  
    用当前 token 的 Query 和所有 token 的 Key 做内积（点积）。  
	    $q_1k_1, q_1k_2, q_1k_3,...$ 得到两个token之间的相关程度
    结果表示：当前 token 对每个位置的“关注度”有多高。  
    得到一个 `5×5` 的矩阵，行列分别代表“我要关注谁”和“谁被我关注”。
    
2. **缩放并归一化**：  
    除以根号下 `head_dim`一般是$\sqrt{d_k}=\sqrt{64}$（避免内积值太大），再用 softmax 把每一行的分数转成概率（和为 1）。
    
3. **加权求和**：  
    用这些==概率权重去乘每个 token 的 Value==，然后求和。  
    这样每个 token 最终的输出就是**所有 token 的 Value 的加权平均**，权重就是注意力分数。
    

这个过程可以写成公式：

$$Attention(Q,K,V)=softmax(\frac{QK^T}{\sqrt{d_k}})V$$

**==每个词都会去问所有其他词“我们有多相关？”，然后根据相关性大小，从其他词那里收集信息来更新自己。==**

---
![[Pasted image 20260604163412.png]]
## 4. 为什么还要搞“多头”注意力？
![[Pasted image 20260604163412.png]]
**Musk掩码让每个词只关注==自己和前面的词==**
如果只有一套 Q/K/V，模型可能只能学到一种“关系模式”。多头注意力就是同时搞多组 Q/K/V（比如 8 个头），让模型可以在==不同子空间里学习不同的关系==：

- 头 1 专注语法关系（主谓宾）
    
- 头 2 专注指代（代词指代谁）
    
- 头 3 专注语义相似性
    

最后把所有头的==结果拼起来==，再==经过一个线性层得到最终输出==。这大大增强了模型的能力。

---

## 5. 顺序去哪儿了？位置编码

既然全部单词同时处理，模型就不知道单词的顺序了（“我爱你”和“你爱我”对它来说是一样的）。因此需要==显式地加入**位置信息**==：给每个位置生成一个==位置向量==，直接加到单词的 embedding 上。

最早 Transformer 用的是正弦/余弦函数生成的位置编码（固定），后来很多模型（包括 GPT 系列）改成可学习的位置 embedding。这样模型就能区分第 1 个词和第 5 个词的差别。

---

## 6. Transformer 的整体积木

原始论文提出了 **Encoder‑Decoder** 结构，但后来业界主要分化成三种变体：

- **Encoder‑only**（如 BERT）：双向上下文理解，适合分类、提取答案。
    
- **Decoder‑only**（如 GPT、DistilGPT2）：单向（因果）注意力，从左到右预测下一个词，适合文本生成。
    
- **Encoder‑Decoder**（如 T5、BART）：常用于翻译、摘要。
    

你现在在 Day 1 里运行的 **DistilGPT2** 就是一个==**decoder‑only** ==模型。它的每一层（Decoder Layer）由两个主要积木组成：

1. **Masked 自注意力**（`GPT2Attention`）  
    与完整自注意力一样，但多了一个 **因果掩码**：不允许当前词看到它后面的词（只能看到自身及之前的内容）。  
    做生成时，我们总是根据已有的前缀预测下一个词，这一点很关键。
    
2. **前馈网络**（`GPT2MLP`）  
    一个简单的两层全连接网络，中间有激活函数（GELU）。可以对每个位置的向量进行更复杂的非线性变换。
    

这两个积木之间都有 **残差连接**（把输入直接加到输出上）和 **层归一化**（保持数值稳定），帮助训练深层网络。

一个完整的推理流程是：

1. 输入 prompt → Tokenizer 拆成数字 ID → 查表得到 token embedding → 加上位置 embedding
    
2. 送入 **N 层**（DistilGPT2 是 6 层）decoder layer，逐层加工
    
3. 最后一层的输出经过 LayerNorm 和一个线性层（lm_head）投影到词汇表大小，得到 logits
    
4. 取最后一个位置的 logits，用 softmax 变成概率，然后采样出一个新 token
    
5. 把新 token 拼到序列末尾，重复第 2–4 步，直到生成完
    

你刚才用的 `with torch.no_grad():` 就是在推理时告诉 PyTorch“不要记录梯度，不要构建反向传播的图”，这样可以节省大量显存和计算时间。

---

## 7. 为什么 Transformer 如此强大？

- **并行性**：训练时整句话一次过，比 RNN 快十几倍。
    
- **长距离建模**：任意两个词直接交互，路径长度是 O(1)。
    
- **极致的可扩展性**：堆积更多层、更多头、更大的 hidden size，模型能力会平滑提升，目前还没有看到明显的天花板。
    

现在绝大多数大型语言模型（GPT‑4、Claude、Gemini 等）都是 decoder‑only Transformer 的不断变种，只是改了注意力机制（如 MQA、GQA）、加了 MoE（后几天的学习内容）或优化了训练方式。你现在运行的 DistilGPT2 就是最经典的、缩微版的 GPT‑2，完美适合理解 Transformer 的本质。
# 2. MoE混合专家模型概念
==**打破传统transformer大规模应用时的performance, cost, model size不可能三角**==
1. 传统Transformer架构是一个稠密模型（dense model）: 包括Multihead-Attention用于收集数据获取相关性和Feedforward Network用于加工和提炼信息，稠密是指对于每一个输入FFN的token，所有FFN层中的参数都要参与计算
2. OLMoE：原本的FFN被替换为了多个小FFN，每个是一个专家负责专门业务，Router作为调度员把不同token分别给对应的一部分专家。由于这种按需激活的特性，我们把MoE model 称为Sparse model稀疏模型
![[Pasted image 20260604170507.png]]
## 跟着一个token的视角来理解：
当token经过attention layer处理后，来到MoE层，首先会进入Router(一个小型的神经网络：比如一个简单的线性层, 对token分析并通过softmax输出一个专家选择倾向概率分布，参数k=2表示我们选择概率最高的两个专家处理token，最后经过Add+Norm: 残差链接+层归一化输出结果

## 为什么选择MoE架构：
1. Scaling Law 在大规模应用中学习速度更快，训练时间更短
2. Impossible Triangle: dense model 无法同时有大的performance, cost, model size
## 常见的一些改进思路：
1. Deepfake MoE: 两个重要的改进思路，
	1. fine grained expert细颗粒度专家：把大而少的专家改为小而多的专家 ，保持总计算资源和时间不变，存在一个临界点sweet point, 当专家数量超过这个时，performance不会继续提升
	2. 引入shared expert共享专家：设置所有token都必须经过的专家，他们不参与router选择。认为不需要从框架上限制哪些专家是共享专家（会限制模型自适应能力），而是在训练中产生一些处理通用任务的专家
2. 从token选专家变为专家选token, 来解决热门专家负载过大问题。但是产生新的问题：1.部分token没有专家选择 2.自回归专家会一个个生成单词，专家无法从不存在的句子中找到topk生成。
3. 总结：工业界主流还是token选专家，会设置一些辅助措施帮助平衡专家负载，比如给一些惩罚函数
## MoE架构的难点缺陷：
MoE训练稳定性是一个很大的问题，训练过程可能开始loss function稳步下降，到某个节点损失值爆发
设计一套稳定可靠的训练策略，让庞大的MoE模型能从头到尾平稳训练是系统工程。
比如特殊的参数初始化方法，精细学习率调整策略，q裁剪

